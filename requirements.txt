pip==25.3
mediapy==1.2.4
numpy==2.1.2
imageio==2.37.0
imageio-ffmpeg==0.6.0
matplotlib==3.10.1
omegaconf==2.3.0
openai==1.76.2
opencv-python==4.11.0.86
pycocotools==2.0.8
scikit-image==0.25.2
tqdm==4.67.1
mmcv==2.2.0
timm==1.0.15
gdown
pandas
reportlab
pyyaml
pillow
# Optional, for the evaluation scripts only:
#   sentence-transformers   # eval_bert.py --model sbert
#   transformers            # eval_bert.py / eval_general.py --model bert
# The 'qwen' backbone needs no extra client-side deps: it is served over the
# OpenAI protocol by vLLM. Install vLLM in a SEPARATE environment (see README).